version: 2.1

orbs:
  ci-tools: kanopi/ci-tools@1.7.1

jobs:
  split_branch:
    docker:
      - image: cibuilds/base
    steps:
      - checkout
      - ci-tools/install-jq
      - run:
          name: Install Git Subtree
          command: |
            apk add git-subtree
      - run:
          name: Push Items
          command: |
            FILE=split-tree.json
            KEYS=$(jq -r '. | keys | .[]' $FILE)
            while IFS= read -r KEY; do
              REMOTE_REPO=$(jq -r --arg key "${KEY}" '.[$key].repo' $FILE)
              SUB_PATH=$(jq -r --arg key "${KEY}" '.[$key].path' $FILE)
              SUB_BRANCH="repo/${KEY}"
              REMOTE_REPO_NAME="${KEY}"

              git subtree split --prefix=${SUB_PATH} -b "${SUB_BRANCH}"
              git remote add "${REMOTE_REPO_NAME}" "${REMOTE_REPO}"
              git push "${REMOTE_REPO_NAME}" "${SUB_BRANCH}":"${CIRCLE_BRANCH}"
              git checkout "${CIRCLE_BRANCH}"
            done \<<< "${KEYS}"
  split_tag:
    docker:
      - image: cibuilds/base
    steps:
      - checkout

workflows:
  build_branch:
    jobs:
      - split_branch:
          context: kanopi-code
          filters:
            branches:
              only: /.*/
            tags:
              ignore: /.*/
      - split_tag:
          context: kanopi-code
          filters:
            branches:
              ignore: /.*/
            tags:
              only: /^v[0-9]+(\.[0-9]+)*$/
